%h1= @pin.title
%p= @pin.description

= link_to 'Back', root_path
= link_to 'Edit', edit_pin_path
= link_to 'Delete', pin_path, method: :delete, data: { confirm: "Are you sure?" } # When this is used after root->pin links, creates two popup boxes...